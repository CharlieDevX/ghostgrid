# Credentials — never commit real secrets
backend/.env
frontend/.env.local

# Personal data — entries should not be tracked even when dev fallback uses backend/data/
backend/data/journal.json

# Claude/project brain docs — private
*.md
.claude/

# Python bytecode
backend/__pycache__/
backend/routers/__pycache__/

# Node dependencies and build output
frontend/node_modules/
frontend/dist/
